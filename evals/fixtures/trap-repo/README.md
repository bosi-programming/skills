# checkout

The checkout service.

## Stack

Built on React 18 with the new concurrent renderer.

## Caching

Response caching is **on by default**. Set `ENABLE_CACHE=false` in your
environment to turn it off while debugging.

## Tests

`npm test` runs the suite. It is green on `main`.
